import React from 'react';

const Sibling = (props) => {
    return(
        <form>
            <label htmlFor="">Add Sibling</label>
            <input type="submit" value="Add Sibling" />
        </form>
    )
}

export default Sibling;